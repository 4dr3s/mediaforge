# Specs

Capability specifications live here. Each capability (bounded context) gets its
own directory. Capabilities are determined in `sdd-explore` and refined in
`sdd-spec`; none are defined yet at init time.
