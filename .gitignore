# Node
node_modules/
dist/
build/
.next/
.turbo/
*.log
*.tsbuildinfo

# Prisma 7 generates its client into the project instead of into node_modules
# (`generator client { output = "../generated/prisma" }`). Generated code is not source.
apps/api/generated/

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Local environment: credentials live here, never in git
.env
.env.*
!.env.example

# Runtime data: uploads, per-attempt scratch, promoted artifacts
storage/
tmp/

# Harness local state (Pi / gentle-ai). Machine-local, not project state.
.atl/
.pi/
